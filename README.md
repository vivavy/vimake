# vimake
 Vi's build system
```sh
# installation
git clone https://github.com/vivavy/vimake.git
cd vimake
sudo ./install
```

# Uninstallation
```sh
git clone https://github.com/vivavy/vimake.git
cd vimake
sudo ./install
```
